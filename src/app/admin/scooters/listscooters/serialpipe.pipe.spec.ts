import { SerialpipePipe } from './serialpipe.pipe';

describe('SerialpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SerialpipePipe();
    expect(pipe).toBeTruthy();
  });
});
